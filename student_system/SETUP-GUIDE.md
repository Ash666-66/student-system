# 🎓 学生选课系统 - 项目设置指南

## 📋 已完成的配置

### ✅ 项目结构
```
student_system/
├── student_env/          # 👈 虚拟环境（已创建）
├── student_system/        # Django项目配置
├── users/               # 用户管理应用
├── courses/             # 课程管理应用
├── templates/            # HTML模板
├── static/              # 静态文件
├── scripts/              # 自动化脚本
├── activate-env.bat      # 👈 Windows环境激活脚本
├── activate-env.sh       # 👈 Linux/macOS环境激活脚本
└── requirements.txt       # 依赖文件
```

### ✅ 虚拟环境状态
- **虚拟环境已创建**：`student_env/`
- **激活脚本已准备**：Windows (`activate-env.bat`) 和 Linux (`activate-env.sh`)
- **基础依赖已配置**：Django, Pillow, python-decouple
- **开发工具已配置**：ruff, black, pytest

## 🚀 快速开始（推荐方式）

### 方法一：使用自动化激活脚本（推荐）

#### Windows用户：
```cmd
# 在项目根目录下运行
activate-env.bat
```

#### Linux/macOS用户：
```bash
# 在项目根目录下运行
source activate-env.sh
```

这个脚本会自动：
1. ✅ 激活虚拟环境
2. 📚 安装项目依赖
3. 🧪 安装开发工具
4. 🗄️ 运行数据库迁移
5. 🎉 完成环境设置

### 方法二：手动步骤

#### 1. 激活虚拟环境

**Windows:**
```cmd
student_env\Scripts\activate
```

**Linux/macOS:**
```bash
source student_env/bin/activate
```

#### 2. 安装依赖

```bash
# 基础依赖
pip install -r requirements.txt

# 开发工具
pip install -r requirements-dev.txt
```

#### 3. 数据库迁移

```bash
python manage.py migrate
```

#### 4. 创建超级管理员

```bash
python manage.py createsuperuser
```

#### 5. 启动开发服务器

```bash
python manage.py runserver
```

## 🌐 访问系统

### 应用地址
- **前端应用**：http://127.0.0.1:8000/
- **管理后台**：http://127.0.0.1:8000/admin/

### 首次使用
1. 访问管理后台创建超级管理员
2. 注册测试用户（管理员、教师、学生）
3. 创建课程和班次
4. 测试学生选课功能
5. 验证成绩录入功能

## 🛠️ 开发工具使用

### 代码格式化
```bash
# 使用ruff格式化
ruff format .

# 使用black格式化
black .
```

### 代码检查
```bash
# 使用ruff检查
ruff check .

# 自动修复可修复的问题
ruff check --fix .
```

### 测试运行
```bash
# 运行所有测试
pytest

# 运行测试并生成覆盖率报告
pytest --cov=.
```

### 数据库操作
```bash
# 创建迁移文件
python manage.py makemigrations

# 应用迁移
python manage.py migrate

# 重置数据库（谨慎使用）
python manage.py flush
```

## 📁 项目功能概览

### 👥 用户管理
- ✅ 多角色注册（管理员、教师、学生）
- ✅ 扩展用户档案信息
- ✅ 基于角色的权限控制
- ✅ 用户列表和详情查看

### 📚 课程管理
- ✅ 课程信息管理（CRUD）
- ✅ 课程班次管理（CRUD）
- ✅ 学期和学年管理
- ✅ 课程容量控制

### 📝 选课功能
- ✅ 学生在线选课
- ✅ 选课申请审核
- ✅ 退课申请处理
- ✅ 选课状态跟踪

### 📊 成绩管理
- ✅ 教师成绩录入
- ✅ 学生成绩查看
- ✅ 成绩统计分析
- ✅ 成绩修改记录

### 📢 公告系统
- ✅ 系统公告发布
- ✅ 课程公告管理
- ✅ 公告状态控制
- ✅ 公告分类显示

## 🔧 故障排除

### 常见问题

#### 1. 激活环境失败
```cmd
# 确保在正确的目录
cd path\to\student_system

# Windows: 检查Scripts目录存在
dir student_env\Scripts

# Linux: 检查bin目录存在
ls student_env/bin
```

#### 2. 依赖安装失败
```bash
# 更新pip
pip install --upgrade pip

# 清理缓存
pip cache purge

# 使用国内镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple/
```

#### 3. 数据库迁移失败
```bash
# 检查迁移文件
python manage.py showmigrations

# 强制重新迁移
python manage.py migrate --fake-initial
```

#### 4. 静态文件不显示
```bash
# 重新收集静态文件
python manage.py collectstatic --noinput

# 检查settings.py配置
grep STATIC settings.py
```

## 📚 进一步开发

### 添加新功能
1. 创建新的Django应用：`python manage.py startapp appname`
2. 定义模型：编辑 `models.py`
3. 创建迁移：`python manage.py makemigrations appname`
4. 定义视图：编辑 `views.py`
5. 配置URL：编辑 `urls.py`
6. 创建模板：在 `templates/` 中添加HTML文件

### 部署到生产环境
1. 配置生产环境变量
2. 安装生产依赖：`pip install -r requirements-prod.txt`
3. 收集静态文件：`python manage.py collectstatic`
4. 配置Web服务器（Nginx + Gunicorn）
5. 设置数据库（PostgreSQL/MySQL）

## 🎯 快速命令参考

```bash
# 开发命令
activate-env.bat          # 激活环境
python manage.py runserver    # 启动服务器
python manage.py shell         # Django shell

# 数据库命令
python manage.py migrate       # 应用迁移
python manage.py makemigrations  # 创建迁移
python manage.py createsuperuser # 创建管理员

# 代码质量
ruff format .              # 格式化代码
ruff check .               # 代码检查
black .                   # black格式化

# 测试命令
pytest                     # 运行测试
pytest --cov=.           # 测试覆盖率
```

## 📞 技术支持

如果在设置过程中遇到问题：

1. **检查Python版本**：确保Python 3.8+
2. **检查网络连接**：依赖安装需要稳定的网络
3. **检查文件权限**：确保项目目录有读写权限
4. **查看错误日志**：终端输出会显示详细的错误信息

---

🎉 **恭喜！您现在拥有了一个完整的现代化Django学生选课系统！**

系统包含：
- ✅ 现代化开发环境配置
- ✅ 完整的用户权限管理
- ✅ 响应式前端界面
- ✅ 完整的业务流程实现
- ✅ 代码质量工具集成
- ✅ 详细的文档和指南

**开始开发吧！** 🚀